<!DOCTYPE html>
<html>
  <head>
    <title>Dot Loyalty</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "vendor", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="font-Nunito text-base font-normal text-gray-600">
    <div class="wrapper overflow-x-hidden min-h-screen bg-gray-50">
      <%= render 'menu' %>
      <main class="container text-sm mx-auto mt-4 px-5 flex" data-controller="<%= controller_name %>">
        <%= yield %>
      </main>
    </div>
  </body>
</html>
